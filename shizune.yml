spring:
  datasource:
    url: jdbc:postgresql://postgres-shizune.parchaa-hip:5432/shizune
    username: parchaa
    password: parchaa123
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 3003

application:
  config:
    naruto: naruto:3002
